import { GameSearchFormModel } from './game-search-form-model';

describe('GameSearchFormModel', () => {
  it('should create an instance', () => {
    expect(new GameSearchFormModel()).toBeTruthy();
  });
});
